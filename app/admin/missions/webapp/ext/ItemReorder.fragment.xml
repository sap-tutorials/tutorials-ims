<core:FragmentDefinition
  xmlns:core="sap.ui.core"
  xmlns="sap.m"
  xmlns:dnd="sap.ui.core.dnd">
  <Table id="itemReorderTable" items="{items}" mode="None">
    <dragDropConfig>
      <dnd:DragDropInfo
        sourceAggregation="items"
        targetAggregation="items"
        dropPosition="Between"
        drop=".onDrop"/>
    </dragDropConfig>
    <columns>
      <Column width="3rem"><Text text=""/></Column>
      <Column><Text text="Order"/></Column>
      <Column><Text text="Type"/></Column>
      <Column><Text text="Task"/></Column>
    </columns>
    <items>
      <ColumnListItem>
        <cells>
          <core:Icon src="sap-icon://menu2" class="sapUiSmallMarginBegin"/>
          <Text text="{items>itemOrder}"/>
          <Text text="{items>taskType}"/>
          <Text text="{= ${items>taskType} === 'TUTORIAL' ? ${items>tutorial/title} : ${items>taskType} === 'GROUP' ? ${items>group/title} : ${items>checkpointTitle} }"/>
        </cells>
      </ColumnListItem>
    </items>
  </Table>
</core:FragmentDefinition>
