{
  "ci": {
    "collect": {
      "url": [],
      "_url_comment": "URLs are injected at runtime via --collect.url=... flags from the deploy.yml a11y-scan job (built from test/a11y/urls.js).",
      "numberOfRuns": 1,
      "settings": {
        "preset": "desktop",
        "skipAudits": ["uses-http2", "is-on-https"],
        "throttlingMethod": "simulate",
        "chromeFlags": "--no-sandbox --headless --disable-dev-shm-usage"
      }
    },
    "assert": {
      "_comment": "Warn-only baseline. No preset (presets bake in audit-level assertions at error severity that override the category-level warn settings below).",
      "assertions": {
        "categories:performance":    ["warn", { "minScore": 0.80 }],
        "categories:accessibility":  ["warn", { "minScore": 0.95 }],
        "categories:best-practices": ["warn", { "minScore": 0.90 }],
        "categories:seo":            ["warn", { "minScore": 0.95 }]
      }
    },
    "upload": {
      "target": "temporary-public-storage"
    }
  }
}
