# SAP Tutorials A2A Agent — Consumption Guide

> **Canonical source:** [`srv/mcp/a2a-instructions.md`](../../../srv/mcp/a2a-instructions.md).
> The guide lives under `srv/` so it is packaged into the deployed CAP module and
> served at `GET /.well-known/a2a-instructions.md` (docs/ is not deployed to the srv
> container — see [[a2a-instructions-md-404-docs-not-deployed]] / #1220). Edit the
> `srv/` copy; this file is a pointer only.

See `srv/mcp/a2a-instructions.md` for the full A2A consumption guide (discovery,
authentication, transport, skill selection, errors, and Admin-UI configuration).
