{
  "name": "tutorials-poc",
  "private": true,
  "type": "module",
  "scripts": {
    "fetch-tutorials": "tsx scripts/fetch-tutorials.ts --target hugo",
    "fetch-tutorials:vitepress": "tsx scripts/fetch-tutorials.ts",
    "fetch-tutorials:hugo": "tsx scripts/fetch-tutorials.ts --target hugo",
    "generate-dark-theme": "tsx scripts/generate-dark-theme.ts",
    "validate-tutorials": "tsx scripts/validate-tutorials.ts",
    "build:apps": "npm --prefix apps run build",
    "build:hugo": "hugo --source hugo --minify",
    "build:display": "cd display-app && npm install && npm run build",
    "build:resilient": "tsx scripts/resilient-build.ts",
    "copy:static": "tsx scripts/copy-static.ts",
    "build:all": "npm run fetch-tutorials -- --regenerate && npm run build:apps && npm run build:hugo && npm run build:display",
    "build": "vitepress build site",
    "dev": "vitepress dev site",
    "dev:hugo": "hugo server --source hugo",
    "preview": "vitepress preview site",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "fundamental-styles": "^0.39.0",
    "gray-matter": "^4.0.3",
    "markdown-it": "^14.1.1",
    "tsx": "^4.0.0",
    "vitepress": "^1.6.0",
    "vitest": "^3.0.0",
    "vue": "^3.5.0",
    "yaml": "^2.7.0"
  },
  "dependencies": {
    "@stomp/stompjs": "^7.3.0"
  }
}
